# SonoGPT 可行性评估与完整架构设计

> 文档状态：方案设计稿  
> 评估范围：当前 SonoGPT 仓库、`chatgpt_chat.txt` 中的讨论、RTX 2060 笔记本约束  
> 项目定位：学习项目，不用于临床诊断或医疗决策

## 1. 结论先行

这个项目可行，但必须把目标说准确：

- 可行的目标是：在 RTX 2060 笔记本上，从零训练一个约 10M～20M 参数的领域小型 Transformer，完成甲状腺超声文本与结构化字段之间的转换，并用规则系统完成报告质控。
- 不可直接声称的目标是：模型“理解医学”、能够诊断甲状腺疾病、可用于真实临床，或者仅凭合成数据达到可证明的临床可靠性。
- 项目最有价值的部分不是追求“大模型”规模，而是完整展示数据定义、Tokenizer、Transformer、训练、严格评估、推理服务和工程复现。
- V1 应只做文本任务。图像到报告属于一个新的数据与验证问题，不应被当作 V1 的自然小升级。
- “报告质控”中能由确定规则判断的内容，应交给规则引擎；模型负责语言到结构的映射。这样比让语言模型同时承担抽取、推理和判错更可靠，也更容易解释。

推荐最终定位：

> SonoGPT 是一个从零训练的轻量级甲状腺超声语言 Transformer，用于研究结构化所见与标准化报告之间的双向转换；系统以确定性规则执行完整性和一致性质控。它是学习与技术演示项目，不用于临床诊断。

名称全部统一为 `SonoGPT`：

- 仓库、Python 包、模型家族、CLI、API 和 Web 界面都使用 `SonoGPT`；
- README、页面标题、模型卡和演示材料保持同一名称。

## 2. 当前仓库审计

### 2.1 实际状态

截至本次评估，仓库中只有：

- `chatgpt_chat.txt`：953 行需求和概念讨论。
- Git 元数据：当前 `main` 与 `origin/main` 一致，只有一个提交。

当前不存在：

- Python 源代码；
- 数据生成器和数据文件；
- Tokenizer；
- 模型定义；
- 训练与断点恢复代码；
- 评估集和指标实现；
- 推理入口或 Web 服务；
- 依赖和环境锁定文件；
- 自动化测试；
- README、许可证、数据卡或模型卡。

因此目前还没有“现有代码架构”可以评价。下文是基于聊天目标给出的目标架构，而不是对已实现系统的描述。

### 2.2 聊天方案中正确的方向

- 把任务限制为甲状腺超声文本，而不是直接做超声图像诊断。
- 采用“结构化字段 → 报告”和“报告 → 结构化字段”的双向任务。
- 使用可控合成数据解决第一阶段无真实病例数据的问题。
- 把标准化、缺失字段提醒和前后矛盾检测作为产品展示点。
- 明确声明项目为研究和演示用途。
- 用小型 Transformer 展示 Tokenizer、Embedding、Attention、因果掩码和自回归生成。

### 2.3 聊天方案中需要修正的判断

1. “可以生成 10 万、50 万、100 万条数据”不等于拥有足够的数据多样性。  
   如果只有几十个模板，100 万条样本仍可能只是重复的字符串拼接，模型会记住模板而不是获得组合泛化能力。

2. 随机按“行”切分数据会造成严重泄漏。  
   同一个语义病例的不同改写不能分别进入训练集和测试集；同一模板家族也应有留出测试。

3. “Attention 关注尺寸，所以模型理解了尺寸”不是可靠结论。  
   Attention 热图可以作为模型检查工具，但不能称为推理过程或可解释性证明。

4. “Accuracy 96.8%”没有意义，除非明确分母和错误类型。  
   JSON 可解析率、字段 F1、数值准确率、遗漏率、幻觉率和质控误报率必须分开报告。

5. 质控不应主要依赖生成模型。  
   缺失字段、单位换算和结构化字段与正文尺寸冲突都适合确定性规则。模型只需要先把自由文本可靠地抽取成结构。

6. 医生纠正后自动回灌训练集存在风险。  
   必须经过脱敏、审核、版本管理和离线评估，不能让线上反馈直接触发持续训练。

7. 多模态 V4 不是简单地增加一个视觉编码器。  
   它需要合法数据、图像质量控制、标注、域外验证和图像—报告配对；这部分的难度可能大于全部文本阶段。

## 3. 产品边界与成功定义

### 3.1 V1 范围

V1 只支持：

- 中文甲状腺超声文本；
- 单个结节；
- 两个模型任务：
  - `extract`：自然语言报告 → 规范 JSON；
  - `generate`：规范 JSON → 标准化报告草稿；
- 一个非模型任务：
  - `qc`：结构完整性、单位、格式和字段—报告一致性检查；
- 本地 CLI 和离线评估。

实现时不要同时起步：

- V1.0 先完成 `generate`，验证数据、Tokenizer、模型、训练和评估链路；
- V1.1 再加入 `extract` 和多任务采样；
- 两者稳定后才接入 `qc` 规则和 Web Demo。

V1 暂不支持：

- 超声图像输入；
- 多结节关系解析；
- 良恶性判断；
- 自动给出诊断或治疗建议；
- 未经明确规则版本化的 TI-RADS 评分；
- 真实医院系统接入；
- 线上自动学习。

### 3.2 可验收的成功标准

在锁定测试集上至少满足：

- 结构化输出 JSON 可解析率不低于 99%；
- 核心字段宏平均 F1 不低于 95%；
- 尺寸数值和单位规范化准确率不低于 98%；
- 报告内容的字段遗漏率和无依据新增率分别单独报告；
- 质控规则在人工构造缺陷集上的每类 Precision、Recall、F1 均有结果；
- “留出模板测试集”与“普通同分布测试集”的结果分开展示；
- 能在 32～128 条样本上过拟合到接近零损失，证明训练链路正确；
- 固定种子后，可从配置和数据清单复现实验；
- 推理输出经 Schema 校验，非法结果不会伪装成有效结果返回。

这些阈值是工程目标，不是临床有效性标准。若达不到，应如实报告失败类型，而不是只展示挑选过的成功样例。

## 4. 总体架构

```text
领域字段定义 + 约束规则 + 文本模板
                │
                ▼
        语义病例生成器
                │
       ┌────────┴────────┐
       ▼                 ▼
结构化 JSON         多样化文本渲染
       └────────┬────────┘
                ▼
     按语义病例/模板家族分组切分
                │
       ┌────────┴────────┐
       ▼                 ▼
 Tokenizer 训练      固定测试与挑战集
       │
       ▼
多任务 Causal LM 数据编码
       │
       ▼
从零训练 Mini Decoder-only Transformer
       │
       ▼
 Checkpoint + Tokenizer + Config + Manifest
       │
       ├───────────────┐
       ▼               ▼
离线评估          本地推理服务
                       │
              Schema 校验与规范化
                       │
              确定性规则质控引擎
                       │
              结果、警告和审计信息
```

### 4.1 核心设计原则

- 模型处理模糊语言，规则处理确定约束。
- 数据先生成“语义病例”，再生成表达方式，不能反过来从模板字符串决定标签。
- 所有结果都从冻结测试集计算，不在 Demo 页面写死数字。
- 模型、Tokenizer、Schema、数据生成器和规则都必须有独立版本。
- 训练产物必须能追溯到代码提交、配置、数据清单和随机种子。
- 不把模型概率直接解释为医学置信度。

## 5. 推荐仓库结构

```text
SonoGPT/
├─ README.md
├─ pyproject.toml
├─ uv.lock                         # 或 requirements.lock，二选一
├─ .gitignore
├─ LICENSE
├─ configs/
│  ├─ data/
│  │  ├─ v1_small.yaml
│  │  └─ v1_full.yaml
│  ├─ tokenizer/
│  │  └─ bpe_4096.yaml
│  ├─ model/
│  │  └─ sonogpt_16m.yaml
│  └─ train/
│     ├─ cpu_smoke.yaml
│     └─ rtx2060.yaml
├─ src/sonogpt/
│  ├─ schemas/
│  │  ├─ domain.py
│  │  └─ io.py
│  ├─ data/
│  │  ├─ ontology.py
│  │  ├─ constraints.py
│  │  ├─ semantic_generator.py
│  │  ├─ renderers.py
│  │  ├─ noise.py
│  │  ├─ split.py
│  │  ├─ manifest.py
│  │  └─ dataset.py
│  ├─ tokenizer/
│  │  ├─ train.py
│  │  ├─ wrapper.py
│  │  └─ validation.py
│  ├─ model/
│  │  ├─ config.py
│  │  ├─ attention.py
│  │  ├─ block.py
│  │  ├─ gpt.py
│  │  └─ generation.py
│  ├─ training/
│  │  ├─ trainer.py
│  │  ├─ checkpoint.py
│  │  ├─ scheduler.py
│  │  └─ reproducibility.py
│  ├─ evaluation/
│  │  ├─ extraction.py
│  │  ├─ generation.py
│  │  ├─ qc.py
│  │  ├─ robustness.py
│  │  └─ report.py
│  ├─ qc/
│  │  ├─ completeness.py
│  │  ├─ consistency.py
│  │  ├─ units.py
│  │  └─ ruleset.py
│  ├─ inference/
│  │  ├─ engine.py
│  │  ├─ validation.py
│  │  └─ cli.py
│  └─ utils/
│     ├─ logging.py
│     └─ hashing.py
├─ scripts/
│  ├─ generate_data.py
│  ├─ train_tokenizer.py
│  ├─ train.py
│  ├─ evaluate.py
│  └─ infer.py
├─ tests/
│  ├─ data/
│  ├─ tokenizer/
│  ├─ model/
│  ├─ training/
│  ├─ evaluation/
│  └─ qc/
├─ data/
│  ├─ fixtures/                    # 可提交的小样本
│  ├─ manifests/                   # 可提交
│  ├─ processed/                   # 不直接提交大文件
│  └─ eval/                        # 冻结且版本化
├─ artifacts/                      # 不提交
│  ├─ tokenizers/
│  ├─ checkpoints/
│  └─ reports/
└─ docs/
   ├─ DATA_CARD.md
   ├─ MODEL_CARD.md
   ├─ EVALUATION.md
   └─ CLINICAL_LIMITATIONS.md
```

推荐采用 `src` 布局，避免脚本从仓库根目录运行时意外导入错误文件。路径操作统一使用 `pathlib`，为 Windows 和 Linux 保持兼容。

## 6. 数据设计

### 6.1 先定义唯一规范 Schema

所有模块都依赖同一个领域 Schema，禁止数据生成、训练和推理各自维护一套字段名。

V1 的单结节逻辑建议仍使用 `nodules` 数组，以免 V2 支持多结节时破坏接口：

```json
{
  "organ": "thyroid",
  "nodules": [
    {
      "location": {
        "side": "right",
        "segment": "middle"
      },
      "dimensions_mm": [8.0, 6.0],
      "composition": "solid",
      "echogenicity": "hypoechoic",
      "shape": "wider_than_tall",
      "margin": "smooth",
      "echogenic_foci": "none",
      "vascularity": "not_mentioned"
    }
  ],
  "lymph_nodes": "not_mentioned"
}
```

关键语义必须区分：

- `not_mentioned`：原文没说；
- `none`：原文明示未见；
- `unknown`：原文存在但无法归一；
- `not_applicable`：该字段在当前情况不适用。

“没有提到钙化”和“明确未见钙化”不能使用同一个空值，否则质控无法判断缺失。

尺寸统一为毫米和数值数组，原始表达如 `0.8×0.6cm` 只保留在输入或审计元数据中。训练目标使用固定键顺序、无多余空格的规范 JSON，降低无意义的格式学习。

### 6.2 数据生成的正确分层

数据生成必须分成四层：

1. 领域本体层  
   定义字段、枚举、同义词、单位和允许的缺失状态。

2. 语义病例层  
   先采样结构化病例，给每个病例分配稳定的 `semantic_case_id`。

3. 文本实现层  
   同一个病例可由不同模板、语序、同义词和省略策略渲染成多条文本。

4. 扰动与缺陷层  
   单独生成错别字、空格、全半角、单位变化、字段遗漏和人为矛盾，用于鲁棒性与质控，不与正常训练样本混为一谈。

每条样本至少记录：

```json
{
  "sample_id": "stable-id",
  "semantic_case_id": "case-id",
  "task": "extract",
  "input": "甲状腺右叶中部见……",
  "target": "{\"organ\":\"thyroid\",...}",
  "template_family": "finding_first_v2",
  "generator_version": "1.0.0",
  "schema_version": "1.0.0",
  "seed": 20260824,
  "source": "synthetic"
}
```

### 6.3 约束与分布

字段不能完全独立随机采样。应建立显式约束，并由领域资料或专业人员确认。例如：

- 尺寸维度个数允许 2 或 3，但格式必须内部一致；
- 不同单位要先转换后比较；
- 文本中的否定词必须绑定到正确字段；
- “边界未见明显异常”不能被简单关键词规则误提取为“边界不规则”；
- 多结节阶段必须保证属性与对应结节绑定，不能把第一个结节的尺寸配给第二个结节。

不要擅自把医学上可能少见的组合删除。所有“非法组合”规则都必须可追溯到明确资料并版本化，否则生成器会把开发者偏见写入数据。

训练分布需要统计并输出：

- 每个枚举值出现次数；
- 缺失状态比例；
- 尺寸范围和单位比例；
- 模板家族比例；
- 各任务样本数；
- 序列长度分位数；
- 重复文本和重复语义病例数量。

### 6.4 数据规模

建议按验证结果逐级扩大：

- `fixtures`：32～128 条，用于单元测试和过拟合测试；
- `small`：5,000～20,000 个语义病例，用于跑通全链路；
- `full`：50,000～100,000 个有足够表达多样性的语义病例；
- 每个病例可渲染少量不同表达，但不能依靠大量近重复改写虚增规模。

在没有学习曲线证明收益前，不建议直接生成 50 万或 100 万条。先画出数据规模与留出模板集性能的关系，再决定是否扩大。

### 6.5 数据切分

禁止直接随机打散所有文本行。

推荐：

- 训练集、验证集、普通测试集按 `semantic_case_id` 分组切分；
- 额外保留从未参与训练的模板家族测试集；
- 额外保留人工编写的挑战集；
- 同一个语义病例的所有改写只能进入一个集合；
- 测试集一旦冻结，不再用于选择模型和调参。

建议测试组成：

- 同分布测试：检验基础学习；
- 组合泛化测试：字段组合未整体出现过；
- 模板留出测试：表达方式未见过；
- 鲁棒性测试：错别字、标点、单位、语序、空白；
- 缺失与否定测试：重点防止 `not_mentioned`/`none` 混淆；
- 反事实测试：只改变一个字段，输出必须只发生对应变化。

### 6.6 数据许可与隐私

V1 使用纯合成数据时，也应提供 Data Card，说明：

- 字段和模板来源；
- 是否参考公开资料及其许可证；
- 生成器版本；
- 已知偏差；
- 不包含真实患者数据；
- 不可用于临床训练或诊断验证。

未来若加入真实报告：

- 必须先确认数据授权；
- 移除姓名、日期、住院号、设备号等可识别信息；
- 不得把真实病例提交到公开 Git 仓库；
- 必须将真实数据与合成数据分开统计；
- 必须由人工审核后才能进入离线训练版本。

### 6.7 数据上传策略

不建议把大批生成 JSONL 和模型权重直接提交到 Git。

推荐上传：

- 数据生成代码；
- 配置和固定随机种子；
- 小型 `fixtures`；
- 数据 manifest；
- 每个数据文件的 SHA-256；
- 压缩数据包的下载位置和版本号。

大数据可使用 GitHub Release、对象存储或 DVC；若数据完全可复现，在家里重新生成往往比上传更简单。模型权重同样放 Release 或模型存储，不进入普通 Git 历史。

## 7. Tokenizer 设计

### 7.1 推荐方案

最终训练使用一个在训练集上自行训练的 SentencePiece BPE Tokenizer：

- 词表大小：4,096；
- 训练语料：仅训练集的输入和目标；
- 中文字符覆盖率：至少 0.9995；
- 启用 byte fallback，避免未登录字符直接变成 `<unk>`；
- 保留数字、单位、乘号、JSON 标点和任务控制符；
- 训练后冻结，不随验证集或测试集变化。

特殊 Token 建议：

```text
<pad>
<bos>
<eos>
<unk>
<task_extract>
<task_generate>
<input>
<target>
```

“自训练 Tokenizer”不要求自己重写一个生产级 BPE 库。为了学习算法，可以另写一个小型 BPE 教学实现和测试，但正式训练建议使用经过验证的实现，避免 Tokenizer Bug 污染全部实验。

这个自定义 Tokenizer 只与从零训练的 SonoGPT checkpoint 配套。若后续增加预训练模型 LoRA 对照，必须使用该基座原有 Tokenizer，不能把 SonoGPT 的 4,096 词表直接替换进去后仍声称兼容预训练权重。

### 7.2 字符级基线

在 BPE 前先建立字符级 Tokenizer 基线，价值在于：

- 中文领域词表小，容易检查；
- 尺寸、单位和 JSON 符号不会因错误预分词丢失；
- 可以快速判断问题来自模型还是 BPE；
- 便于做 32 条样本过拟合测试。

BPE 必须与字符级基线比较：

- 平均序列长度；
- 词表覆盖；
- 往返编码一致性；
- 核心字段准确率；
- 推理速度和显存。

### 7.3 Tokenizer 验收

必须通过：

- `decode(encode(text))` 对规范化文本可逆；
- 数字、小数、`×`、`x`、`*`、`mm`、`cm` 不丢失；
- 中文全角/半角标点有明确规范化策略；
- 特殊 Token 不会由普通文本意外生成；
- 保存 Tokenizer 文件的哈希；
- 推理加载时核对 Tokenizer 哈希与 checkpoint 中记录一致；
- 在测试集统计 `<unk>` 率，目标为 0 或接近 0。

## 8. 模型设计

### 8.1 为什么选择单个 Decoder-only Transformer

从任务效率看，Encoder-Decoder 很适合双向文本转换，字段抽取也可以由 Encoder 分类模型完成。但本项目的首要目标是从零学习 GPT/Transformer，所以建议使用一个多任务 Decoder-only Causal LM：

- 架构统一，便于完整实现和讲解；
- `extract` 与 `generate` 由任务 Token 区分；
- 可以演示因果掩码、自回归训练和生成；
- 参数量和显存都适合 RTX 2060。

需要如实说明：这是教学选择，不代表它是生产环境中最省算力或最可靠的任务架构。

### 8.2 输入序列格式

抽取任务：

```text
<bos><task_extract><input>甲状腺右叶中部见……<target>{"organ":"thyroid",...}<eos>
```

生成任务：

```text
<bos><task_generate><input>{"organ":"thyroid",...}<target>甲状腺右叶中部见……<eos>
```

训练时：

- 模型输入包含任务、输入和目标；
- 只对 `<target>` 之后的 Token 计算交叉熵；
- Prompt 和 Padding 对应标签设为 `-100`；
- 每种任务独立统计损失和指标；
- 任务采样保持平衡，避免样本多的一方主导训练。

### 8.3 推荐配置

建议的 `SonoGPT-16M`：

```yaml
vocab_size: 4096
max_seq_len: 384
n_layers: 8
n_heads: 6
d_model: 384
d_ff: 1536
dropout: 0.10
norm: pre_layer_norm
activation: gelu
position_embedding: learned_absolute
tie_embeddings: true
bias: false
```

该配置约为 16M 参数，实际数量必须由代码计算并打印，不能把聊天中的 `18.7M` 当成既定事实。

设计细节：

- 每个 Attention Head 维度为 64；
- 使用 Pre-LN 提高小模型训练稳定性；
- 输出 Head 与输入 Embedding 权重共享；
- 使用 Causal Mask 和 Padding Mask；
- 初始化方案写入配置并加入测试；
- 首版使用可读性高的标准实现，不为了追求性能过早引入复杂内核。

### 8.4 Attention 实现与展示

建议保留两条执行路径：

- 正常训练路径：使用 PyTorch SDPA 可用的安全后端；
- 调试路径：返回指定层、指定头的 Attention 权重，用于测试和可视化。

可视化必须标记为“Attention inspection”，不能标记为“模型思维过程”或“医学解释”。可以使用遮挡实验或反事实输入辅助检查：删除尺寸后，尺寸输出应明显变化；只改变左右叶时，其他字段不应变化。

### 8.5 为什么 V1 不直接采用更大的预训练模型

0.5B 级模型的 4-bit 微调在部分 6GB 设备上可能运行，但会引入：

- Windows、量化库和显卡架构兼容性；
- 更复杂的显存优化；
- 项目重点从“理解并实现 Transformer”转为“调用微调框架”；
- 更难区分模型先验与本项目数据带来的能力。

因此主线应从零训练 16M 模型。后续可以把小型预训练模型 LoRA 作为对照实验，而不是替换主线。

## 9. 训练设计

### 9.1 RTX 2060 约束

RTX 2060 笔记本通常是 6GB 显存，但必须在目标电脑上用 `nvidia-smi` 确认实际显存、驱动和温度限制。

推荐起始配置：

```yaml
precision: fp16
micro_batch_size: 4
gradient_accumulation_steps: 16
effective_batch_size: 64
max_seq_len: 384
optimizer: adamw
learning_rate: 0.0003
weight_decay: 0.1
warmup_ratio: 0.03
scheduler: cosine
max_grad_norm: 1.0
gradient_checkpointing: false
num_workers_windows: 0
```

这只是安全起点，不是承诺值。先运行 200～500 Step 基准：

- 若显存有余量，逐步提高 micro-batch；
- 若 OOM，先降低 micro-batch，再考虑激活检查点；
- 不要通过缩短输入而静默截断目标；
- 记录每秒 Token、峰值显存和 GPU 温度；
- 笔记本长时间训练需注意供电、散热和系统休眠。

RTX 2060 使用 FP16，不应把 BF16 设为默认。FlashAttention 2 等优化不能假设支持 Turing/Windows，正常 PyTorch 路径必须能独立运行。Windows DataLoader 入口需要 `if __name__ == "__main__":` 保护。

### 9.2 训练阶段

阶段 A：数据与规则基线

- 验证 Schema；
- 建立模板生成基线和规则抽取基线；
- 生成小数据；
- 确认切分无泄漏。

阶段 B：最小正确性测试

- 用字符级 Tokenizer；
- 用更小的 2 层模型；
- 对 32～128 条样本过拟合；
- 检查 Loss、Mask、Padding、Checkpoint 和生成终止。

阶段 C：小规模端到端实验

- 训练 `SonoGPT-16M`；
- 使用 5,000～20,000 个病例；
- 比较字符级和 BPE；
- 验证指标代码和断点恢复。

阶段 D：完整训练

- 冻结 Tokenizer、Schema 和测试集；
- 扩大到 50,000～100,000 个病例；
- 根据验证集早停；
- 保留最佳 checkpoint 与最后 checkpoint；
- 只在模型选择结束后运行最终测试。

### 9.3 优化与损失

- 损失函数：目标 Token 的 Cross Entropy；
- 首版不使用 Label Smoothing，避免降低精确 JSON Token 的确定性；
- `extract` 和 `generate` 分别计算平均损失；
- 若任务长度差异大，按样本而不是简单按 Token 数检查任务权重；
- 使用梯度裁剪；
- 学习率通过小规模实验选择，不照搬大模型配置；
- 早停依据语义综合分数，不能只看验证 Loss。

建议综合分数优先级：

1. JSON 可解析率；
2. 核心字段 F1 和尺寸准确率；
3. 无依据新增率与遗漏率；
4. 验证 Loss。

### 9.4 Checkpoint 与可复现

Checkpoint 至少包含：

- 模型权重；
- 优化器状态；
- Scheduler 状态；
- AMP GradScaler 状态；
- Epoch、Global Step；
- 当前最佳指标；
- Python、NumPy、PyTorch 随机状态；
- 模型和训练配置；
- Tokenizer 哈希；
- 数据 manifest 哈希；
- Schema、生成器和规则版本；
- Git commit。

启动训练时打印完整环境：

- Python、PyTorch、CUDA、cuDNN；
- GPU 型号和显存；
- 操作系统；
- 是否启用确定性算法；
- 实际参数量。

必须测试：从某个 checkpoint 恢复后，后续 Loss 与未中断训练在允许误差内一致。

## 10. 评估设计

### 10.1 必须先有基线

至少比较：

- 规则/正则抽取基线；
- 确定性模板报告生成基线；
- 字符级小模型；
- BPE `SonoGPT-16M`。

模板生成对规范 JSON 几乎可以做到零幻觉，因此模型生成报告的价值主要是语言变化能力。若模型在语义正确性上不如模板，应保留模板作为产品默认或回退路径，而不是为了展示模型而隐藏基线。

### 10.2 抽取任务指标

分别报告：

- JSON 可解析率；
- Schema 校验通过率；
- 整条结构 Exact Match；
- 每字段 Precision、Recall、F1；
- 宏平均和微平均 F1；
- 尺寸维度个数准确率；
- 尺寸数值准确率；
- `cm → mm` 单位转换准确率；
- `not_mentioned`、`none`、`unknown` 混淆矩阵；
- 多余字段率；
- 核心字段遗漏率。

数值指标需要同时提供：

- 严格精确匹配；
- 允许浮点表示误差的容差匹配；
- 不允许用大容差掩盖单位转换错误。

### 10.3 报告生成指标

BLEU/ROUGE 只能作为次要指标，因为不同措辞可以表达同一语义。

主要指标应为：

- 生成报告再经独立解析器或规则抽取后的字段一致率；
- Slot Error Rate：
  - 缺失；
  - 错误；
  - 重复；
  - 无依据新增；
- 尺寸、部位和否定表达准确率；
- Schema 到文本的反事实一致性；
- 非法或越界内容率；
- `EOS` 正常结束率；
- 人工可读性抽样。

不能只用同一个被评模型完成“生成后再抽取”的闭环评价，否则两个模型错误可能互相抵消。至少需要规则解析器、人工标注集或独立模型作为交叉检查。

### 10.4 质控指标

为每类人工注入错误分别统计：

- Precision；
- Recall；
- F1；
- False Positive Rate；
- 错误定位准确率。

缺陷类型至少包括：

- 缺少成分；
- 缺少回声；
- 缺少形状；
- 缺少边界；
- 缺少强回声特征；
- 结构与正文尺寸不一致；
- 左右叶不一致；
- 单位不统一；
- 同一字段自相矛盾；
- 明确否定被误认为缺失。

### 10.5 鲁棒性与泄漏检查

必须比较以下集合的性能差：

- 训练模板同分布；
- 留出模板；
- 人工新写语句；
- 标点和空格扰动；
- `×`、`x`、`*`；
- `mm` 与 `cm`；
- 同义词；
- 语序变化；
- 错别字；
- 超长输入和截断边界；
- 空输入、乱码和恶意超长输入。

泄漏检查：

- 训练/验证/测试的 `semantic_case_id` 无交集；
- 规范化后的文本哈希无交集；
- 测试模板家族不在训练集；
- 生成器统计不读取测试答案来调分布；
- Tokenizer 不用测试集训练。

### 10.6 评估报告

每次正式实验输出机器可读 JSON 和 Markdown 报告，包含：

- 数据版本；
- 模型和 Tokenizer 配置；
- Checkpoint 哈希；
- 全部指标；
- 各字段结果；
- 失败样例分类；
- 延迟、吞吐、峰值显存；
- 与基线和上一个版本的差异。

Demo 页面只读取评估产物，不手工填写参数量、样本数或准确率。

## 11. 推理设计

### 11.1 处理链

`extract`：

```text
原始文本
  → Unicode/单位/空白规范化
  → Tokenizer
  → Greedy 自回归生成
  → JSON 解析
  → Schema 校验
  → 数值和枚举规范化
  → 返回结构 + 校验警告
```

`generate`：

```text
结构化 JSON
  → Schema 校验
  → 规范序列化
  → 模型生成报告草稿
  → 报告字段一致性检查
  → 合格则返回；不合格则警告或使用模板回退
```

`qc`：

```text
报告文本 + 可选结构化数据
  → 模型抽取结构
  → 规则检查缺失项
  → 规则比较尺寸/部位/枚举
  → 返回逐项证据和人工确认提示
```

### 11.2 解码策略

- 结构化抽取默认 Greedy，温度概念上为 0；
- 报告生成默认 Greedy，演示语言多样性时才允许很低温度；
- 禁止为结构化任务使用高温采样；
- 限制最大新 Token；
- 遇到 `<eos>` 终止；
- 后续可增加 JSON Grammar Constrained Decoding；
- JSON 失败最多执行一次有记录的确定性重试，仍失败就返回明确错误；
- 不应静默用正则“修好”任意非法 JSON 后当作模型成功。

### 11.3 规则引擎

规则引擎独立于模型并版本化：

- 必填项检查；
- 字段状态区分；
- 单位换算；
- 尺寸和部位一致性；
- 范围和格式检查；
- 文本与结构的证据定位；
- 规则严重度；
- 规则 ID 和可读说明。

如后续增加 TI-RADS，只能：

- 选定并注明一个明确指南及版本；
- 用确定性、可单元测试的规则实现；
- 为每条规则建立来源和边界案例；
- 与语言模型解耦；
- 明确标为研究演示结果，不作为诊断建议。

### 11.4 接口与审计

第一阶段优先 CLI，训练和评估稳定后再加 FastAPI。

建议的 V1 API 契约：

```text
GET  /healthz
GET  /v1/model-info
POST /v1/extract
POST /v1/generate
POST /v1/qc
```

- `/healthz` 只报告服务是否可用；
- `/v1/model-info` 返回模型、Tokenizer、Schema 和规则版本，不暴露本地路径；
- `/v1/extract` 接受报告文本，返回通过 Schema 校验的结构或明确失败；
- `/v1/generate` 接受规范结构，返回报告草稿、语义校验状态和是否触发模板回退；
- `/v1/qc` 接受报告及可选结构，返回规则 ID、证据、严重度和人工确认提示；
- 请求和响应使用版本化 Pydantic Schema，并限制文本长度和请求体大小。

每次推理记录非敏感审计信息：

- request ID；
- 模型、Tokenizer、Schema、规则版本；
- 输入长度和是否截断；
- 解码参数；
- Schema 校验结果；
- 推理耗时；
- 警告和回退原因。

默认不永久保存用户输入。若未来记录医生修正，必须先做授权、脱敏和人工审核。

V1 直接加载 PyTorch checkpoint 即可。TorchScript 或 ONNX 只作为性能/分发扩展；在没有延迟或部署证据前，不应为了“技术栈完整”提前增加导出链路。

## 12. 测试策略

### 12.1 数据测试

- 同一种子生成结果完全一致；
- Schema 始终有效；
- ID 稳定且无重复；
- 约束规则覆盖边界；
- 单位换算正确；
- 切分无语义病例泄漏；
- 模板留出逻辑有效；
- manifest 哈希可复算。

### 12.2 Tokenizer 测试

- 往返编码；
- 特殊 Token；
- 数字和单位；
- 未登录 Unicode；
- Padding、Truncation；
- 训练和推理加载一致；
- Tokenizer 与 checkpoint 不匹配时拒绝运行。

### 12.3 模型测试

- 张量形状；
- Causal Mask 不泄漏未来 Token；
- Padding 不影响有效 Token；
- 权重共享；
- Loss Mask 只覆盖目标；
- 单步生成；
- EOS 终止；
- KV Cache 与无 Cache 结果一致；
- 保存后加载输出一致。

### 12.4 训练测试

- 32 条样本过拟合；
- 梯度非零且无 NaN；
- AMP 下稳定；
- 梯度累积等价性；
- Checkpoint 恢复；
- 固定种子复现；
- CPU Smoke Test；
- 单 GPU Smoke Test。

### 12.5 评估与推理测试

- 每个指标用手算小样本验证；
- JSON 解析失败计入失败，不能跳过；
- 批量和单条推理一致；
- 空输入、超长输入、非法 JSON；
- 规则误报边界；
- API 不泄露栈信息或本地路径。

## 13. 现在可以完成的准备工作

在没有 RTX 2060 的当前环境中，可以完成大部分工作：

1. 固定项目边界、Schema 和字段词典。
2. 实现语义病例生成器、模板渲染器、扰动器和约束验证器。
3. 实现按语义病例和模板家族的切分。
4. 生成 `fixtures`、`small` 和冻结挑战集。
5. 生成数据统计与 manifest。
6. 训练并验证字符级和 BPE Tokenizer。
7. 实现 Mini Transformer、Loss Mask、生成和 checkpoint。
8. 在 CPU 上完成单元测试和 32 条样本过拟合。
9. 实现全部评估指标和规则基线。
10. 准备 RTX 2060 配置，但不提前假设最佳 batch size。
11. 锁定依赖，并把代码、配置、小数据和 manifest 上传。
12. 将大数据压缩包和 SHA-256 放到 Release/对象存储，或在家中按 manifest 重新生成。

真正需要回家后 GPU 完成的工作主要是：

1. 用 `nvidia-smi` 记录 GPU、显存和驱动。
2. 安装与驱动匹配的 PyTorch CUDA 构建。
3. 跑 GPU Smoke Test。
4. 跑 200～500 Step 性能和显存基准。
5. 调整 micro-batch，不改变有效实验定义。
6. 运行完整训练并验证断点恢复。
7. 对最佳 checkpoint 执行一次冻结测试集评估。

换句话说，不需要等回家才开始。数据、Tokenizer、模型代码、评估和 CPU 正确性验证都可以提前做好；GPU 只负责把已经验证正确的训练任务跑完。

## 14. 训练前的交付包

上传前应能得到以下内容：

```text
代码提交
├─ 可锁定的 Python 依赖
├─ 所有配置
├─ 单元测试通过记录
├─ CPU Smoke Test 记录
├─ 数据生成器
├─ fixtures
├─ full 数据 manifest 与哈希
├─ Tokenizer 文件与哈希
├─ 冻结测试集版本
├─ 未训练模型参数统计
└─ 一条可恢复的训练命令说明
```

不要上传：

- `.venv`；
- CUDA 安装目录；
- Python 缓存；
- 临时日志；
- 未筛查的真实患者文本；
- 大型 checkpoint 到普通 Git；
- 任何账号密钥。

## 15. 主要架构风险与处理优先级

### 15.1 当前阻断项

1. 仓库没有实现  
   当前只有讨论文本，尚不存在可运行架构。

2. Schema 未冻结  
   如果字段、空值语义和单位未先确定，数据、Tokenizer、模型和指标都会反复重写。

3. 没有独立 Gold/Challenge 测试集  
   只使用同一个生成器产生训练和测试数据，会高估泛化能力。

4. 没有基线和指标定义  
   无法判断 Transformer 是否比规则和模板更有价值。

### 15.2 高风险

1. 合成数据模板泄漏  
   处理：按语义 ID 分组切分，留出模板家族，加入人工挑战集。

2. 把合成任务成绩包装成医学能力  
   处理：明确只证明在受控 Schema 和生成分布上的映射能力。

3. 用生成模型做确定性质控  
   处理：模型抽取，规则判定，显示证据。

4. JSON 非法或字段幻觉  
   处理：规范序列化、目标专用 Loss、Greedy、Schema 校验、失败显式返回、后续约束解码。

5. 缺失与明确否定混淆  
   处理：Schema 分开表示并建立专项测试。

6. 多结节属性串位  
   处理：V1 明确只支持单结节，V2 单独设计关系和索引评估。

7. 测试集被反复用于调参  
   处理：验证集负责选择，测试集冻结并只在最终运行。

8. 反馈自动回灌  
   处理：脱敏、审核、版本化、离线训练和回归评估。

9. 把 Attention 当作因果解释  
   处理：只作为检查，并配合反事实和误差分析。

10. Windows/显卡优化库不兼容  
    处理：保留纯 PyTorch 路径，优化作为可选项。

### 15.3 中风险

1. 一开始生成过多数据，降低迭代速度。  
   处理：先做学习曲线。

2. 词表过小导致罕见字符和单位丢失。  
   处理：byte fallback、覆盖率测试、字符级基线。

3. 只看 Loss，不看结构语义。  
   处理：按任务保存语义指标。

4. 报告生成模型不如模板可靠。  
   处理：诚实比较，模板可作为默认或回退。

5. 笔记本热降频导致训练时间不可预测。  
   处理：短基准、温度记录、频繁 checkpoint。

6. 从零小模型对人工真实语言泛化有限。  
   处理：人工留出集、失败分析；后续增加预训练模型对照，但不改变主线目标。

## 16. 推荐实施里程碑

### M0：规格冻结

交付：

- 项目边界；
- Schema v1；
- 字段本体；
- 风险声明；
- 指标定义。

通过条件：同一个示例可无歧义地表示为规范 JSON，并能区分缺失和明确否定。

### M1：数据与规则基线

交付：

- 生成器；
- 切分；
- manifest；
- 规则抽取/模板生成/质控基线；
- 人工挑战集。

通过条件：无切分泄漏，统计可复现，规则测试通过。

### M2：Tokenizer 与模型最小闭环

交付：

- 字符级基线；
- BPE Tokenizer；
- 2 层 Debug 模型；
- 16M 正式模型；
- 32 条过拟合。

通过条件：Loss Mask、Causal Mask、保存加载和生成全部验证。

### M3：RTX 2060 训练

交付：

- GPU 基准；
- 完整 checkpoint；
- 可恢复训练日志；
- 最佳模型选择记录。

通过条件：无 OOM/NaN，断点恢复有效，验证指标稳定。

### M4：冻结评估

交付：

- 与规则、模板、字符级模型的对比；
- 普通、模板留出和挑战集报告；
- 错误分类；
- Data Card 与 Model Card。

通过条件：所有数字可从评估脚本重新生成，不手工挑样例。

### M5：推理与学习展示

交付：

- CLI；
- FastAPI；
- Web Demo；
- 模型实验页；
- 限制声明。

通过条件：所有输出经过校验；失败有明确状态；页面中的指标来自真实评估产物。

### M6：可选扩展

按价值排序：

1. 多结节；
2. 小型预训练模型 LoRA 对照；
3. 经审核的少量真实文本外部测试；
4. 人工反馈离线数据闭环；
5. 多模态研究。

## 17. 项目应如何描述

推荐：

> 这是一个从零实现并训练的约 16M 参数 Decoder-only Transformer，用任务 Token 完成甲状腺超声报告与规范结构之间的双向转换。数据管线先生成语义病例再生成多种语言表达，并按语义病例和模板家族切分，避免合成数据泄漏。模型只负责语言映射，完整性和一致性质控由版本化规则完成。项目在 RTX 2060 笔记本上训练，并用字段 F1、尺寸准确率、遗漏率、幻觉率及模板留出集进行评估。

不推荐：

- “这是一个医疗大模型”；
- “模型能判断甲状腺癌”；
- “Attention 证明模型像医生一样思考”；
- “有 100 万条数据，所以效果可靠”；
- “准确率 96.8%”，但无法解释这个数字。

## 18. 最终建议

立即开始准备是合理的，而且大部分准备工作不需要 GPU。正确顺序不是先批量造 100 万条数据，也不是先写漂亮 Web 页面，而是：

```text
冻结 Schema
  → 建立生成器和防泄漏切分
  → 建立规则/模板基线
  → 冻结挑战集
  → 验证 Tokenizer
  → 让极小模型过拟合
  → 完成评估和断点恢复
  → 回家跑 GPU 基准
  → 正式训练
  → 最后做 Demo
```

这个范围在 RTX 2060 笔记本上是现实的。它的学习价值取决于是否能严谨地展示“为什么这样设计、如何防止数据泄漏、模型在哪些样本上失败、为什么规则与模型要分工”，而不是取决于把模型叫作“大模型”或生成多少条近重复数据。
